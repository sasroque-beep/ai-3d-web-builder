# Copy this file to `.env.local` and fill in real values.
# `.env.local` is git-ignored. Never commit real secrets (AGENTS.md §13).
#
# No runtime environment variables are required yet. As modules are
# implemented, document their variables here (name + short description),
# keeping this file the single source of truth for configuration.

# Public base URL of the app, used for canonical URLs and metadata.
# NEXT_PUBLIC_SITE_URL="http://localhost:3000"
