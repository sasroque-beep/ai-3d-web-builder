# Enforce the package manager declared in package.json "packageManager".
# Run `corepack enable` once so pnpm is provisioned automatically.
engine-strict=false

# pnpm: keep the dependency tree strict (no phantom deps).
auto-install-peers=true
strict-peer-dependencies=false
